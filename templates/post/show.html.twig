{% extends 'base.html.twig' %}

{% block title %}Postari{% endblock %}

{% block body %}


    <h1 align="center" >Postari</h1>
    <table class="table" >
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Titlu</th>
                <td>{{ post.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ post.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_post_index') }}">Inapoi</a>
    <a href="{{ path('app_post_edit', {'id': post.id}) }}">Edit</a>
    {{ include('post/_delete_form.html.twig')}}

{% endblock %}
